{
  "name": "cafeworking-site",
  "version": "0.34.0",
  "private": true,
  "description": "Site e plataforma inicial do CafeWorking",
  "scripts": {
    "dev": "netlify dev",
    "serve": "python -m http.server 8888",
    "check": "node scripts/check-links.js",
    "build": "node scripts/build-dist.js",
    "test": "node --test scripts/*.test.js",
    "vitrine": "node scripts/vitrine.js",
    "cidades": "node scripts/paginas-cidade.js"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}
